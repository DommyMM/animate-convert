# agents.md

Project context for AI agents lives in [CLAUDE.md](CLAUDE.md).

`CLAUDE.md` is the source of truth. Both files exist because different tools look for different names (Claude Code reads `CLAUDE.md` automatically; `agents.md` is a common convention for other agents). They should be kept in sync — when updating one, update both.

Symlinks weren't possible without admin rights, so these are duplicate files.
